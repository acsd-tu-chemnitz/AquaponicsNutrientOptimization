Info on running the code:

- The script RAS_stationaryOpt.m first runs the steady state optimal nutrient and pH management for a fixed feed formulation and follows with a dynamic optimization. The results of both optimizations are compared to validate the steady state approximation
	- Outputs are prompted in the command window

